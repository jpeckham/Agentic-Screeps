# Integration Tests

Integration tests belong here when a behavior spans build artifacts, scripts, or workflow-facing command boundaries. They must not call the live Screeps API during normal verification.
